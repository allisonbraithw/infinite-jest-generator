schema {
  query: Query
  subscription: Subscription
}

type Character {
  fullName: String!
  alternativeNames: [String]
  description: String
  portraitLink: String
}

type Query {
  character(fullName: String!): Character
}

type Subscription {
  timeOfDay: String
}

